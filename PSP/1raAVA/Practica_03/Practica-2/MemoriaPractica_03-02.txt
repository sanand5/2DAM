Que 
Para que:
Pseudocodigo:
Como:
Para empezar, en esta practica he creado dos archivos opcion.cpp y app.cpp.
Opcion.cpp es un programa que imprime una opcion random (piedra/papel/tijeras) y devuelve su indice. 
Primero declaro la semilla del srand y despues creo un entero aleatrio entre 0 y 2 para despues con un if darle un valor a un string, que va a ser impreso, dependiendo de el valor i despues devuelvo ese valor.
app.cpp tiene una clase Juego que tiene dos funciones jugar(publica) y es ganador(privada), esganador() devuelve un indice para saber si es ganador=0, empate=1, o perdedor=2.
En la funcion juego declaro un pid_t para el pipe, un array de dos posiciones que hace referencia a un filedescription par el pipe y un array de chars que es el buffer y otro array de chars que guarda la opcion del usuario.
Primero creo el pipe y gestiono si hay algun posible error, luego llamo a la función fork para crear procesos hijos con la ayuda ifs else anidados calculo si ha havido un error o el proceso en el que estoy es el padre o el hijo. 
Si estoy en el proceso hijo cierro la lectura en el pipe ejecuto el programa opcion con la ayuda de la funcion system divido su retorno entre 256 (lo divido por 256 ja que la funcion system me devuelve el valor por 256, creo que es por system devuelve tambien el estado de finalicacion del proceso i lo almacena en bits) i lo guardo en una variable, luego creo un array de chars donde guardo una opcion de el array opciones para escribirlo en el pipe i cierro la escritura en el pipe.
Si estoy en el proceso padre cierro la escritura en el pipe pregunto al usuario por una opcion la guardo en un sting leeo el pipe y cierro la lectura i duevuelve el valor de lo que devuelce la funcion esGanador con la buffer y la opcion de el usuario como parametros.
Mi funcion declaro un objeto de tipo Juego y dos valores enteros que hacen referencia a los puntos de la maquina o de el usuario luego creo un for de 3 iteraciones donde en el llamo a la funcion jugar de el objeto, i segun el resultado, con un if compruebo quien ha ganado saco el mesaje respectivo i le sumo un punto a quien alla ganado, en caso de enpate solo saco el mensaje respectivo.
Despues del bucle compruebo los resultados y saco el mensaje correspondiente a el.

Concluion: